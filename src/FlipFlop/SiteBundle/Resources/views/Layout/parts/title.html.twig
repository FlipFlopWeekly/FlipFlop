{# Avec ce template on pourras overrider le title pour chaque page, il faut juste ajouter le nom du node current et recuperer le nom 
	Par exmaple:
		FlipFlop - Beta Symfony | {{ node.name }}
#}
FlipFlop - Beta Symfony